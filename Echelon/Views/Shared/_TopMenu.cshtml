@using Echelon.Resources.Content
@{
    var user = Request.GetOwinContext().Authentication.User.Identity.Name;
}

<nav class="navbar navbar-default">
    <div class="container-fluid">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="#">@SiteDocument.SiteTitle</a>
        </div>
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav">
                <li>
                    <a href="#">@SiteDocument.Categories</a>
                </li>
            </ul>
            <form class="navbar-form navbar-left">
                <div class="form-group">
                    <input type="text" class="form-control" placeholder=@SiteDocument.SearchCategories>
                </div>
                <button type="submit" class="btn btn-default">@SiteDocument.Find</button>
            </form>
            <ul class="nav navbar-nav navbar-right">
                <li class="dropdown active">
                    @if (string.IsNullOrEmpty(user))
                    {
                        <span>@SiteDocument.NotLoggedIn</span>
                    }

                    else
                    {
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
                            <span>@user</span>
                            <span class="caret"></span>
                        </a>
                    }

                    <ul class="dropdown-menu">
                        <li>
                            <a href="#">@SiteDocument.Profile</a>
                        </li>
                        <li role="separator" class="divider"></li>
                        <li>
                            <a href="#">@SiteDocument.SignOut</a>
                        </li>
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</nav>